[package]
name = "rbml"
version = "1.0.0"
authors = ["The Rust Project Developers"]

[lib]
name = "rbml"
path = "lib.rs"

[dependencies]
std = { path = "../libstd" }
serialize = { path = "../libserialize" }
log = { path = "../liblog" }

[dev-dependencies]
test = { path = "../libtest" }
